# 京张先通 / PASSAGE BEFORE BARRIERS

正式叙事以 `proposal.md` 与 `proposal.en.md` 为准。本辅助索引把成果链固定为：问题与非 AI 基线 → 占用前证据门 → 运行中证据失效和退化 → 占用后具名恢复 → 90 日人工/AI 对照试验 → AI 无净增益即退出。

The authoritative narrative is `proposal.md` plus `proposal.en.md`. The deliverable chain is: public problem and non-AI baseline → pre-occupation evidence gate → active expiry and degradation → named restoration → 90-day human/AI comparison → AI exits when it adds no net value.

- 结构化证据：`sources.json`、`assumptions.json`、三份专业矩阵。
- 可复算空间：`geometry/*.geojson` 与 `metrics.json`；全部 provisional 或 conceptual 的限制保持可见。
- 可复现实验：`simulation.json` 与 `visual/assets/passage-contract.json`。
- 可读成果：中英双语图件、A3 册、A0 展板、报告 HTML 和离线展陈页。
- 一票否决：未经具名人工核验 READY、证据过期仍 READY、无授权写成真实试点、AI 替代专业放行。
